{
  "version": "1.2.0",
  "download_url": "https://github.com/v2matosevic/talkty-mac/releases/latest",
  "release_notes": "Hardening release: the transcription timeout now actually cancels a stuck run (cooperative abort, engine never wedges), model downloads stream in chunks (much less CPU during downloads), less redraw work while transcribing, and the whole codebase is clean under Swift strict concurrency. Adds two opt-in experiment flags (audio_ctx sizing, debug logging on installed builds)."
}
